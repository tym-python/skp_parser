---
name: skp-parser
description: 解析给定目录下的文件(含 pdf、docx、xlsx),提取省重点项目及相关详情存入 MySQL 数据库,并将原文件上传 GoFast,入库数据与上传文件通过 file_id 关联。当用户要求解析重点项目文件目录、提取各省重点项目清单、解析项目入库或上传 GoFast 时使用。
---

# 省重点项目解析入库

解析指定目录下的 `pdf / docx / xlsx` 文件,提取"省重点项目"名称及相关详情写入 MySQL;
原文件按平台规则上传 GoFast 获得访问 URL;每个项目的入库记录通过 `file_id` 与上传文件关联。

## 触发场景

- 用户给出一个目录(或要求使用默认目录 `util/file_config.py`),目录内含重点项目文件
- 用户要求把某省某年的重点项目清单解析入库
- 用户要求解析的文件同时上传到 GoFast 并在库中留存关联

## 项目结构(按功能分层)

```
skp_parser/
├── main.py                  # 主入口:扫描目录 → 解析 → 上传 → 入库 → 汇总报告
├── parser/                  # 解析器
│   ├── __init__.py          # parse_file 统一入口(按扩展名分发)
│   ├── base_parser.py       # 抽象基类:表头定位/分类上下文/行级兜底/质量过滤
│   ├── field_mapping.py     # 表头别名映射 + 金额/年份清洗 + 特有列收集
│   ├── pdf_parser.py        # PDF(pdfplumber,表格优先 + 质量门)
│   ├── docx_parser.py       # DOCX(python-docx,表格优先)
│   ├── xlsx_parser.py       # XLSX(openpyxl,遍历全部 sheet)
│   └── notice_parser.py     # 红头文件(计划通知类)元信息解析
└── util/                    # 通用工具
    ├── db_util.py           # AsyncDB(aiomysql 连接池,平台选库)
    ├── db_config.py         # local / db252 连接配置
    ├── go_fast.py           # GoFast 上传(aiohttp)
    ├── file_config.py       # 默认目录 + 上传开关 UPLOAD_ENABLED
    ├── log_util.py          # 彩色日志 get_logger
    ├── http/                # HttpRequest 异步上下文管理器
    └── area_id.py           # 省份 ID 常量
```

## 表结构(MySQL,4 张表)

```sql
-- 文件表:每个被解析的原文件一条记录,记录 GoFast 关联
CREATE TABLE IF NOT EXISTS skp_file (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  file_name     VARCHAR(255)  NOT NULL COMMENT '原始文件名',
  file_path     VARCHAR(512)  DEFAULT '' COMMENT '目录内相对路径',
  file_type     VARCHAR(10)   DEFAULT '' COMMENT '扩展名 pdf/docx/xlsx',
  gofast_url    VARCHAR(512)  DEFAULT '' COMMENT 'GoFast 上传后 URL',
  area_id       INT           DEFAULT 0 COMMENT '省份ID,见 util/area_id.py',
  year          INT           DEFAULT 0 COMMENT '文件对应年份,0=未知',
  file_size     BIGINT        DEFAULT 0 COMMENT '文件字节数',
  file_hash     CHAR(64)      DEFAULT '' COMMENT 'SHA-256,用于去重',
  status        TINYINT       DEFAULT 1 COMMENT '0=失败 1=成功',
  created_at    DATETIME      DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_hash (file_hash),
  KEY idx_area_year (area_id, year)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 项目表:解析出的省重点项目明细,file_id 关联 skp_file
CREATE TABLE IF NOT EXISTS skp_project (
  id                    BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  file_id               BIGINT UNSIGNED NOT NULL COMMENT '关联 skp_file.id',
  area_id               INT           DEFAULT 0 COMMENT '省份ID',
  project_name          VARCHAR(255)  NOT NULL COMMENT '项目名称',
  construction_unit     VARCHAR(255)  DEFAULT '' COMMENT '建设单位/项目法人',
  location              VARCHAR(255)  DEFAULT '' COMMENT '建设地点',
  total_investment      DECIMAL(20,2) DEFAULT 0 COMMENT '总投资(万元)',
  annual_investment     DECIMAL(20,2) DEFAULT 0 COMMENT '年度计划投资(万元)',
  start_year            INT           DEFAULT 0 COMMENT '开工年份',
  end_year              INT           DEFAULT 0 COMMENT '竣工年份',
  construction_content  TEXT COMMENT '建设内容',
  responsible_unit      VARCHAR(255)  DEFAULT '' COMMENT '责任单位/牵头单位',
  project_owner         VARCHAR(255)  DEFAULT '' COMMENT '项目业主/业主单位(业主方)',
  source_row            INT           DEFAULT 0 COMMENT '源文件中的行号(调试用)',
  project_type          VARCHAR(32)   DEFAULT '' COMMENT '建设性质:新建/续建/竣工投产/预备/储备',
  category              VARCHAR(128)  DEFAULT '' COMMENT '行业分类(如 工业-电子信息,多级用-连接)',
  annual_goal           VARCHAR(512)  DEFAULT '' COMMENT '年度工作目标(前期研究阶段项目)',
  raw_fields            JSON COMMENT '未映射字段的原始数据',
  created_at            DATETIME      DEFAULT CURRENT_TIMESTAMP,
  KEY idx_file (file_id),
  KEY idx_name (project_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 文件特有信息表:非共性字段,每项目一行 JSON(如 内蒙古表的"合作方式/联系方式")
CREATE TABLE IF NOT EXISTS skp_file_extra (
  id           BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  file_id      BIGINT UNSIGNED NOT NULL COMMENT '关联 skp_file.id',
  project_id   BIGINT UNSIGNED NOT NULL COMMENT '关联 skp_project.id(该行项目)',
  extra_info   JSON COMMENT '如 {"合作方式":"合资","联系方式":"xxx"}',
  created_at   DATETIME      DEFAULT CURRENT_TIMESTAMP,
  KEY idx_file (file_id),
  KEY idx_project (project_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 红头文件(计划通知类)信息表
CREATE TABLE IF NOT EXISTS skp_notice (
  id               BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  file_id          BIGINT UNSIGNED NOT NULL COMMENT '关联 skp_file.id',
  title            VARCHAR(255)  DEFAULT '' COMMENT '通知标题',
  notice_no        VARCHAR(128)  DEFAULT '' COMMENT '文件编号(如 桂重大办〔2020〕4号)',
  issuer           VARCHAR(255)  DEFAULT '' COMMENT '发文单位',
  issue_date       DATE NULL COMMENT '发文时间',
  content          MEDIUMTEXT COMMENT '通知正文',
  attachment_names VARCHAR(1024) DEFAULT '' COMMENT '文末附件名称',
  has_project_list TINYINT       DEFAULT 0 COMMENT '是否含重点项目清单(0=否 1=是)',
  created_at       DATETIME      DEFAULT CURRENT_TIMESTAMP,
  KEY idx_file (file_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

表结构演进:新列通过 `init_db()` 的 `_ensure_column` 自动迁移补列(不改动已有数据)。

**skp_file.full_field_status(JSON)**:记录 full_field 宽松规则的逐 sheet 统计
(解析时由 XlsxParser 收集,main 入库时写入),供人工分析统计口径/阈值:
```json
{"sheets": {"Sheet1": {"relax": false, "full_field_sheet": false, "plain_category_file": true,
  "full": 115, "total": 160, "hinted": 4, "plain": 41,
  "biz_fields": ["construction_content", "project_owner", "responsible_unit"]}}}
```
- `relax`: 本 sheet 是否启用词表外分类行放宽(full_field_sheet and plain_category_file)
- `full/total`: full 率统计(有业务文本行/有名称或业务的行)
- `hinted/plain`: 无业务文本的潜在分类行中带/不带暗示条件的数量
- `biz_fields`: header_map 命中 FULL_FIELD_NAMES 的业务字段
(垫江前期类分类层级多的文件 full 率被分类行稀释 <80%,统计入库供分析后再定口径)

## 执行流程(概览)

```
扫描目录(递归,过滤 ~$ 临时文件,仅 pdf/docx/xlsx)
  └─ 每个文件:
       1. SHA-256 去重(已存在则跳过)
       2. 解析项目记录(解析库同步,asyncio.to_thread 防阻塞)
       3. 通知类文件额外解析通知元信息(skp_notice)
       4. 上传 GoFast(按平台/开关)
       5. 单事务入库:skp_file + skp_project(+ skp_file_extra)
       6. 汇总报告:成功/失败/跳过/项目总数/失败清单
```

**单文件失败不中断整体**,错误记入报告;失败清单输出到日志供人工检查。

## 解析细则(按文件类型)

### 通用:字段映射与清洗(`field_mapping.py`)

- **统一清洗管道 `parser/text_clean.py`**:去首尾空白 → 全角数字/字母转半角 →
  折叠内部空白(clean_text/collapse/halfwidth)。docx 单元格与段落、通知文本等
  统一走该管道,避免各处零散 strip/re.sub 漂移(渐进收敛中,parse_lines 的
  折叠等调用点将逐步迁移)
- **压缩包不解析**:真实格式探测(detect_real_ext)对 ZIP(PK)按内部部件区分
  xlsx(xl/)/docx(word/)/pptx(ppt/);rar/7z/gzip 魔数与**非 OOXML 的 zip**
  (改名伪装压缩包)→ 返回 ".zip" 并明确报错不做解析,不再按扩展名兜底交给
  openpyxl/python-docx(曾误报 "openpyxl does not support .docx");
  扫描入口 collect_files 本身只收 pdf/docx/xlsx 扩展名
- **表头构造**:合并方向为"上一行(表头行)自身横向合并(父标题,如 "2020年计划" 合并跨列)
  + 下一行短文本子表头(每格 ≤30 字)才合并并下移数据起始行";**概况文字行**
  (如 绵阳 "2025年全市城建工程项目共376个…",长文本/句子)→ 不并入表头、跳过不产生项目;
  **父标题仅认横向合并单元格**(merged_ranges 提供时,上方非合并短文本不作父标题;
  防 列宽行 "25"/附件编号 等误拼,如 北京 project_name 被挤到第二列)
- TODO: 分类多行纵向合并且首行与首个项目同行(无 category 映射列)的场景暂未找到
  对应文件,待实际文件出现后处理
- **表头定位(find_header 两遍扫描)**:优先命中 ≥2 字段的行 = 完整表头——备注行
  (如 大足 "注:项目名称标注▲…" 单格长文本仅命中 project_name)不构成表头;
  第二遍兜底:仅命中 project_name 且**非单格长文本**(>30 字)的行(如 山东
  "序号|项目名称" 两列表、单列 "项目名称" 清单仍命中)
- **垂直拆词表头行**(主表头无横向合并时的两行表头,如 垫江前期 "建设起"+下行
  "止年限" / "至2025年底已"+"完成的前期工作"):表头行下方行的非空单元格对应
  主表头同列也非空、每格为短文本(≤8 字、非纯数字、非 合计/总计 开头)、
  **至少 2 列拆词**(单格性质标题如 "新建项目(1项)" 不触发)→ 表头词拆两行,
  直接拼接("建设起止年限" → year_range)并下移数据起始行(拆词行非数据);
  与"父-子 子表头"(header_merged 横向合并,如 湖北)互斥;
  父标题向上拼接排除**纯数字行**(列宽行,如 北京 row1 "5|25|20|20|50",
  数字会被当父标题拼成 "25-项目名称" 把 project_name 挤到第二列)
- **年投资计划合并表头组合判定**(match_field,先于通用别名):父标题含
  `投资计划|计划投资|年度投资` 的组合表头(如 广东 "2021年投资计划-主要建设内容",
  父合并跨列 + 子表头 小计/主要建设内容/新增生产能力):
  子含 `主要建设内容|建设内容` → **annual_goal**(年度建设内容);
  子含 `新增生产能力|新增生产` → 不映射(留 extra,防误当金额);
  子含 `小计|合计` → **annual_investment**;
  无子词("2021年计划投资" 等)→ 走通用别名,不受影响
- **project_name 候选列**:主列(表头修正后)取空时回退候选列——候选 = 修正前后列 +
  数据行横向合并覆盖 project_name 及左邻列(如 芜湖 "B14:C14" 名称在左格、宁夏
  表头修正后主列反而为空);仅限**左邻一列**(防宽合并 "A:K" 把远处未映射列误当候选);
  取值要求:非空、非纯数字(序号/金额)、非占位符、含中文;候选列须未映射其他字段
  (防字段值串用,如 宁夏 "投资来源" 列的 "银行贷款" 不误取)
- **概况文字处理**:分类行名称列带统计说明(如 "一、市本级 项目59个。其中…")→ 分类名
  截断到句号/"其中"/空格分隔的量词短语前("一、创新能力提升项目26个" 等无空格量词不截);
  "（一）市住建委 项目14个，2025年计划完成4.36亿元" 等含句子标点且超长的子分类行
  → 跳过,不产生项目、不设分类
- **数据行检测(header_map 列语义)**:业务字段列(header_map 映射字段排除 项目名称/建设内容,
  如 责任单位/建设性质/建设年限/项目业主 等)含非纯数字文本 → 数据行,不参与分类判断
  (classify_single_row/apply_category 跳过)——建设内容或项目名中的 "（N个）"
  (如 开州"浦里新区加油站（9个）"、渭南建设内容)不会触发兜底误判;分类行的
  "数量+金额"为纯数字单元格(含千分位逗号,如 "1,584,946.56")不触发;
  纯数字、占位符、numCell = re.fullmatch(r'[（(]?\s*共?\s*\d+\s*(?:项|个|件)?\s*[)）]?' ,v)，不是有效业务数据
  (如 补充，['一', '续建项目', '33个', None, None, 8524816])
- **合并单元格分类列识别(cat_scope)**:前提是 header_map 存在 category 映射列
  (表头命中 category 别名,如 "项目类型"/"领域分类")且该列有数据行合并单元格
  (分类信息存于合并首格,如 内蒙古 col0"项目类型" / col1"（一）xxx" 纵向合并)。
  识别范围 = 覆盖 category 列或其相邻未映射列的合并块所占列(限数据行区域 rlo > header_idx,
  排除文件标题行合并如 "A1:D1");不再无差别处理所有 col0/col1——旧逻辑会把建设性质行
  (如 河南 "续建项目(597个)" 在名称列)误识别为分类上下文,污染大量项目分类
  (河南 "续建项目" 656 条),cat_scope 收窄后项目恢复真实类别字段值
- **词表外分类行宽松规则(_relax)**:文件级双条件——数据行 ≥80% 含业务字段文本
  (FULL_FIELD_NAMES:建设单位/地点/建设内容/责任单位/业主/年度目标/建设年限,非纯数字)+
  无业务文本的潜在分类行中**含统计暗示者 <50%**(若分类行普遍带 "（共N项）" 等
  暗示则靠暗示识别,不启用宽松;广东式 "-N/（五）" 层级序号分类行靠剔除性质行
  暗示后的统计启用);行级无业务文本 + 子名 ≤12 字 → m2/m3/
  classify_single_row 的**词表失败点放宽为分类**(保留挂载语义,如 垫江
  "（二） 国省道" → "交通-国省道";广东 "（五）港航工程"、"−1 信息基础设施项目"
  等词表外分类行不再当项目,三级 "-N" 归入罗马同级挂载语义);
  "（三）李家岩水库" 等无 _relax 仍判数据行。超 12 字的分类名补
  CATEGORY_Supplement_WORDS(全等匹配)覆盖。
  说明行(如 开州附件2 "下列12个子项目")循环前部直接过滤,不产生项目、不参与分类
- **装饰行统一判定(备注/正文/概况句,base_parser 的 is_prose_row + 文本级
  is_note_text/_is_overview_sentence)**:不产生项目、不参与分组/分类,各解析器
  同一位置生效(xlsx/xls 行循环非业务路径、pdf/docx 表格、文本兜底 parse_lines):
  - **备注/注记行**:整行文本行首 = 备注/注/附注/注释/说明/特别说明/注记 + 冒号
    (容忍空格如 "备 注:")的表尾/页眉说明(如 重庆 2025 市级重点项目 "备注:标"★"的
    为2025年重大项目。"、绵阳 "注:******为涉密…"、开州附件 "备注:…概算批复为准")
    ——仅认整行行首标签,数据行的建设内容列含 "备注:…" 时行首是项目名不受影响
  - **招商简介正文排版行**:单文本格且 字段标签(PROSE_LABEL,如 "项目名称：…"/
    "联 系 人：…")或 ≥150 字(广西(第一批) 目录后正文区)
  - **清单概况/总结句**:如 奉节 2025 "重大前期项目92个,总投资494亿元。其中,
    政府投资项目54个,总投资102.4亿元;社会投资项目38个,总投资391.6亿元。"、
    渝北 2025 "项目总数246个(新开工73个、续建73个、完工62个、重大前期38个)"、
    奉节 2023 "…共85个,总投资338…"(前言/汇总整句)——特征:去空白 >20 字且
    ①"N个/项/件" 后跟 句子标点/顿号/右括号/其中/总投资/亿元/万元 或行尾 +
    句子成分词(，。；其中/总投资/亿元/万元),或 ②"…N个(" 前缀 + 右括号收尾、
    括号内 ≥2 段顿号分隔的 "N个" 枚举(渝北式,无句读/总额词也判)。
    防误伤:纯列举名称("…年产300万个轴承、500万个齿轮项目" 无句读/总额词、
    非括号枚举式)与 "…(年产30万个)技改项目"(括号内无数段)不命中;
    单文本格行由 is_prose_row 拦截,多格/带序号概况行由 xlsx 非业务路径的
    _is_overview_sentence 兜底;parse_lines 中概况句不作用于数字序号开头行
    (真项目行可能含 "…个,总投资…")
  - **位置无关且不触碰 category**:装饰行判定先于一切 分组/性质/分类 识别
    (continue 前不写 context);表头之前(封面/前言区)的装饰行也由文本级
    is_decor_text 判定直接跳过、**不喂 apply_category**——防止概况句带括号统计等
    hint 被当分类行污染后续项目 category(如 "项目总数246个(新开工73个、…)" 若
    含 "（N个）" 形态会被门控放行设分类)
- **_relax 文件级统计的续行处理**:项目占据多行的文件(如 宁夏 2020:第二行仅
  "投资来源+金额",名称/序号/业务字段全空)→ 统计时跳过(名称候选列空 且 业务字段
  空的行不参与 total/full),否则续行稀释 full 率,宽松规则无法启用;
  名称判定用候选列(pname_candidates:修正后主列+原始列+左邻合并列),
  名称在候选列的行不误跳过
- **_relax 文件级统计的口径分离**:无业务文本的**潜在分类行**(如 垫江前期
  "（一）灌区改造" / "合计(共115项)",首列非纯数字)计入 hinted/plain
  (宽松启用的暗示比例依据),**不占 full 率分母**——否则分类层级多的文件
  (分类行数量多)full 率被稀释 <80%,词表外分类行(县乡道路/水库建设/
  水土保持/通用航空 等)无法经 _relax 放宽;无业务但首列为纯数字序号的行
  视为残缺数据行,仍占分母(防误放宽)
- **占位符清洗**:纯破折号单元格("——"/"--")视为空——先清洗再匹配识别(不入
  non_empty/joined、不触发数据行检测、字段映射后全破折号置空/项目名全破折号过滤),
  防分类行("续建项目110个 | —— | —— |…")因占位符被误判为数据行
- **统计汇总表识别(无项目 sheet,如 泸州 汇总表)**:表头区(前 20 行)命中
  合计/总计/个数/总数/小计 且**不含** 项目名称/项目名/建设内容/建设规模/项目清单
  (项目特征用去空格文本匹配,表头 "项 目 名 称" 带空格不误判;
  总投资/年度投资/建设地点 不参与判定——项目表也有)→ 不入库,记入 noise 名单
  (reason 区分 "统计汇总表" 与 "各省汇总表")
- **表头诊断(analy/)**:find_header 未匹配字段但有表头信号(≥2 非空单元格)的 sheet
  → 日志输出表头 + 追加记录 `analy/sheet_headers_diagnose.txt`(按 sheet 去重),
  暂走两列式兜底;积累后人工完善 FIELD_ALIASES/汇总信号。
  分析产物统一放 `analy/` 目录(noise_sheets.json、scan_report.json、表头诊断);
  TODO: 暂未发现"统计汇总但含真项目"的文件,若出现需改判定逻辑
- **带圈序号**("① 交通项目7个")= 三级分类(与罗马序号同级挂载);行尾数字量词
  ("前期准备项目135个")→ 性质词开头设 project_type,其他设 category
- **分格类别|项目数**(如 浙江"千项万亿":序号列+领域分类列+名称列"项目数 426 个"):
  入口数字剔除保留**量词前导数字**("项目数 426 个" 的 "426" 不剔,否则量词判断失效),
  分类名清洗剥离 "项目数 N 个" 短语

- 表头别名 → 标准字段,别名用完整词防误配;短别名("名称/项目名/工程名/进度目标")
  精确匹配,防"项目名单/进度目标责任表"等复合词误判
- 表头文本清洗:去空格/换行/**连字符**(合并表头生成 "开工-时间"/"建设-地点" → 正常匹配)
- **单位类列名分组**(语义相同者合并,无法区分者入 extra):
  - `construction_unit`(建设方):建设单位/实施单位/项目单位/项目法人/建设主体/法人单位
  - `project_owner`(业主方,单独一列):项目业主/业主单位/业主
  - `responsible_unit`(责任方):责任单位/牵头单位/项目主管单位/监管单位/主管部门/主管单位
  - 项目实施主体:语义介于两者之间,无法可靠区分 → 入 extra
  - 同组多列并存时非空优先(如 建设单位 与 项目法人 同现)
- 建设规模类列(建设规模/建设规模和内容/拟建设规模/建设规模及内容)→ construction_content;
  建设阶段/建设批次 → project_type(值如"续建/在建/一期"等原样存储);
  行业分类/项目类型 → category;计划工期 → year_range(可解析年份区间时)
- "金额单位:万元"/"单位：万元" 备注行不与列标题/数据混合(跳过)
- **全数字字段过滤**:项目名全数字 → 整条过滤;location/project_type/category/
  construction_unit/responsible_unit/project_owner 全数字 → 置空
- **年份+清单类标题行过滤(_is_year_list_title,filter_blank_projects)**:整名含
  "20xx年/年度" 且以 清单/名单/目录/总表 收尾(允许括号类别后缀,如 "2026年上海市
  重大工程项目清单(预备项目)"、"安徽省2026年重点项目清单(B类)"、"2026年市级重点
  储备项目清单"、"云南省2026年度第一批"重中之重"项目清单"、"2026年省重点前期工作
  项目清单")→ 过滤。只按"清单类词收尾"判定,名称**含年份**但不以清单类结尾的真
  项目不受影响(渝北 2025 名单曾因按 "含YYYY年" 过滤被误吞 13 条含年份真项目)
- **表头列错位修正**:表头"项目名称"与数据列偏移(数据列 0 为序号"1.0"、真实项目名在列 1,
  如 云南"四个一百"/宁德) → 两级修正:
  1. **合并表头检查优先**:表头行横向合并覆盖 project_name 列 → 右移一列
  2. **数字列检测兜底**:project_name 列数据 ≥80% 纯数字且下一列有文本 → 右移一列
- **xlsx 大文件限制**:超过 5MB → 提示并跳过解析(人工拆分/优化后重跑;小文件非 read_only
  加载以支持合并单元格检查)
- **"跳过解析"标记**:文件名或所在文件夹名含"跳过解析"的文件不解析(扫描时过滤)
- 金额清洗:支持 "120.5亿元"/"120000万元"/"120,000";表头带单位时裸数字按表头单位换算
  ("总投资额(亿元)" 列值 0.69 → 6900 万元);规整 2 位小数防浮点误差
- **年计划投资区分**:表头含显式年份且非文件年份(如 湖北"2019年计划投资"文件年份 2019)
  的年度投资列 → 入 extra;文件年份由文件名/路径提取
- 数量词排除:"2000万件/5万亩/10万千瓦/1000万立方米" 的"万"不是金额单位
- 年份提取:"2023"、"2023-2025"、"2023年至2025年"、"2024年10月" → start_year/end_year;
  **Excel 日期序列号**(openpyxl 对日期单元格返回数字,如 45597 → 2024)自动转换
- 未映射列(表头有文本、非"序号/编号")→ 收集进 `extra_fields`(文件特有信息)
- 项目类别/产业类别 列 → category(优先级高于分类标题上下文)
- 建设性质列 → project_type

### 分类上下文(category / project_type)

**分类判断基于分类名单**(`_looks_like_category`,`base_parser.py` 的 `CATEGORY_STRONG_WORDS`/`CATEGORY_WEAK_WORDS`),
替代早期的长度判断(长度误判 "(1)李家岩水库" 为分类):

- 含"(N项/N个)" → 分类
- 纯建设性质标题("新建项目/续建项目/投产项目")→ project_type
- 命中**强词**(纯行业大类:基础设施/产业/交通/民生/社会事业/市政…)→ 分类
- 命中**弱词**(具体设施:公路/铁路/高速/水利/光伏/物流/房地产…)+ 文本 ≤6 字 → 分类
  (弱词避免 "G7611…高速公路 凉山州"、"智慧物流园项目" 等长项目名误判)
- **补充词全等无前缀放行**(CATEGORY_Supplement_WORDS):即使无 序号/括号/量词
  等前缀(update_category_context 前置暗示门控对 强/弱词 仍要求结构前缀防误伤,
  名称/建设内容可能含 "交通/市政" 等词),行文本(剔除空格分隔的纯数字计数后)
  整体 = 补充词 → 直接设分类——如 银川 2022 计划表 "一产项目 4"(计数 4 无量词)
  → 分类 "一产项目",同一文件 二产项目/三产项目 同理;分类声明按计数记录(一产 4 项)
- 否则 → 项目名

**分类名统一清洗**(`_clean_category_name`):去前后空格/引号、去开头序号/顿号
("（一）房地产" → "房地产"、"、续建项目" → "续建项目"、"'2+4'产业链项目" → "2+4产业链项目")、
**去合并序号范围**("428-442 4.房地产开发" → "房地产开发","-442" 残留去除,要求含破折号防误伤"2+4")、
去括号统计("（172个）")、去省略号("基础设施…")、性质词后截断("…抽水蓄能电站 新建 长阳县" → "…抽水蓄能电站")、
**去同行金额数字**("前期储备项目 2265.58 838742…" → "前期储备项目",空格分隔的行尾数字序列,
"2+4产业链项目" 等中间数字不误截)。
"明细" 类分类不设置;"性质+内容混合"标题("、续建项目-湖北长阳…")→ 只设 project_type。
"金额单位:万元"/"单位：万元" 说明行不作为项目。

### 责任单位/地区分组行(A2 动态识别)

按责任单位分块的清单(如 广西"南宁市人民政府|16|1528601.56|177000" 行)不作为项目:

- **判定时机(group 与分类检测同路径)**:先按 header_map 列语义**排除业务数据行**
  (`_is_business_data_row`:业务字段列含非纯数字文本 = 真项目行,直接映射为项目,
  不参与任何识别),非业务行再依次识别 **分组行 → 建设性质标题 → 分类行**——
  多列责任表里名称尾"区"的真项目行(如 "…铜梁中学新校区")即使行内只有数字列,
  只要含业务字段文本就不再被分组判断误弃
- 多列映射表(header_map ≥2 列)分组行(is_group_header_row)特征(全满足):
  - 首列(序号列)为空
  - 名称列非空且含中文(纯数字列排除),且以机构后缀结尾
    (人民政府/政府/市/区/县/厅/局/委/办/管委会/集团/公司/学院/研究院,
    如 "南宁市人民政府"/"区水利局";"…快充站建设工程" 等数据行不误判)
  - 非空列数 **≤ max(2, normal_cols-2)**,normal_cols 为同 sheet 普遍数据行的
    非空列数众数(动态对比,自适应不同列数的文件)
  - 计数特征:"N项" 或 纯小整数 1-999(含 float 形式 "4.0"/"16.0";排除 "2021" 年份)
  - 识别后跳过(不产生项目);PDF 表格路径与 xlsx/xls 循环均生效
- 单格分组行(整行排数字/占位后唯一文本,`is_group_row`)覆盖:
  - 区划后缀 **县/州/盟/省 任意长度** + 区域级词(市直/区直,如 贵港 "市直|97|金额")
  - **市/区 后缀:短名(≤ GROUP_CITY_MAX_LEN=5,先去单元格内对齐空格,如 广西目录
    "南宁市"、正文变体 "南 宁 市"→3)且行内无任何数字单元格(无序号/无金额)**——
    城市分组行无编号;编号清单中的短名行是真项目:"963|君澜府小区"、"206|汉中
    综合保税区"、"…灌区"(名称外有 序号/金额 数字列)。全库 REGEXP '市$|区$' 审计
    可见大量 园区/小区/社区/校区/院区/灌区 真项目名——仅长度限制会误弃 4-5 字
    小区/园区(福州 2022 责任表 "君澜府小区" 曾被 ≤5 误当分组,已按"行内有数字"
    恢复)
  - **机构名结尾 ≤14 字**(如 广西责任表 "自治区教育厅|12|金额" 分组行,机构名在
    序号列——否则候选列回退会把机构名当项目名;不含 学院/医院/学校/研究院,
    "xx商务中心" 类真项目名由业务字段判断识别)
  - **场所名尾"厅"例外**:餐厅/展厅/音乐厅/宴会厅/舞厅/客厅/饭厅/茶厅/大厅/礼堂
    结尾是真项目(GROUP_VENUE_TAIL_RE,如 南通 "骏升新能源汽车城市展厅"、福州
    "…未来智慧餐厅"),不按"厅"机构后缀分组;classify_single_row 机构单格分支同步排除
- **单行表头(极简清单,header_map ≤1 列,如 汉中 "序号|项目名称"、广西(第一批)
  目录)窄化分组识别 `is_group_row_minimal`**:这类表无多列分组行形态,但城市/地区
  分组行仍以单格短名出现(广西(第一批) 目录 "南宁市"、正文 "南 宁 市" 曾整体当
  项目入库,category='Table')。多列 group 检测若直接套用,单格真项目名(汉中
  "206|汉中综合保税区" 尾"区")会被误弃——窄化规则(仅单格行)仅认:
  - 机构/区划后缀:人民政府/政府/厅/局/委/办/管委会/县/州/盟/省(任意长度,
    **不含 集团/公司/学院/研究院**——单格公司名如 "…有限公司" 是真项目;同样
    排除 场所名尾"厅")
  - 市/区 短名(同 ≤5 去空格 + 行内无数字规则);**allow_city=False 时该分支整体关闭**
    ——sheet 完全无编号行(纯名称单列清单)不存在城市分组行,"xx小区/园区" 一律保留
    (allow_city = 本 sheet 是否出现过编号行,由循环预扫描计数传入)
  pdf/docx 表格路径(extract_rows_from_table)与 xls 循环同步生效
- **装饰行统一判定 `is_prose_row`(备注/正文/概况句合并,base_parser,各解析器共用;
  详见"通用:字段映射与清洗"的 装饰行 一条)**:
  - 招商简介正文("目录+每项目一页简介"版式,如 广西(第一批).xlsx)按"单列多行"
    排版:单文本格且 正文标签开头(PROSE_LABEL)或 ≥150 字(超长段落/续段)
  - 备注/注记说明行(行首 备注/注/…+冒号)
  - 清单概况/总结句(如 奉节 2025 "重大前期项目92个,总投资494亿元。其中,政府
    投资项目54个…"——无"共"字也识别;_is_overview_sentence)
  均不产生项目、不参与分组/分类;多格/带序号概况行另有非业务路径兜底
- **目录+正文 单行表头 sheet 的"正文区"整体跳过(预扫描,xlsx/xls)**:上述文件
  正文区(目录编号结束后)除标签/超长行外还有 目录名重排行、跨行续段碎片、落款行
  (联 系 人/电话/邮编/项目有效期)等各类单列噪声——逐行过滤总有漏网。预扫描:
  header_map ≤1 列 且 已见 ≥5 个含纯数字序号的行 后,出现 ≥3 个连续无序号行、
  **且该 run 之后不再出现编号行** → 视为进入正文区,正文区首行起整区不入库
  (编号中段夹 3+ 连续分类标题的清单因"之后仍有编号行"不会误伤;多列表不启用;
  预扫描同时产出编号行计数供 is_group_row_minimal 的 allow_city 使用)
- **建设性质标题行统一识别 `group_type_title`**(base_parser,合并原 is_build_type_row
  与 xlsx_parser._group_type_title——两者用途相同即"性质标题 → project_type 上下文",
  仅行形态不同,合并后消除重复词表与漏网形态):
  - 形态1 单格纯词:整行排数字/占位后唯一文本 = 性质词(新建/续建/在建/预备/新开工/
    计划开工/投产/竣工/储备/前期,如 安徽 "续建"/"计划开工" 标题行)
  - 形态2 性质分组行:序号列空 + 项目名称列全词匹配 "性质词+项目"(计划新开工/新开工/
    竣工投产或部分竣工投产/续建/预备/储备/前期/投产/竣工,如 贵港
    "计划新开工项目|19|金额";文件可能无建设性质列,性质由分组行提供)
    ——计划新开工→新开工、竣工投产或部分竣工投产→竣工投产
  - 单格 "续建项目(597个)" 等带量词行不命中形态1(非纯词)→ 由 classify_single_row 处理

标题行处理:
- "一、工业(321项)" → category="工业"(容忍 "一 工业" 空格变体)
- **罗马数字序号等同汉字**(如 四川表 "Ⅰ 机场（9项）" 等同 "一、机场(9项)"、"（Ⅱ）铁路" 等同 "（二）铁路");
  序号后分隔符允许 顿号/圆点/空格 等价("Ⅳ.基础材料(16个)" 等同 "Ⅳ基础材料(16个)",贵阳 2026);
  单元格内跨行文本(如 "港口、码头、航运\n（7项）")归一为空格再匹配
- **【】强调包装去壳(_unwrap_cat_bracket)**:行首 【X】 与无括号等价——"【预备】(1个)"
  等同 "预备(1个)";两入口(update_category_context / classify_single_row)统一处理
- **性质分组头+括号项数(不产生项目)**:如 "预备(1个)"、"【预备】(4)"、"【收尾】(8个)"
  (贵阳 2026 docx,性质词含 收尾)→ 只设 project_type,性质纯词不入 category
- **分类等级**:汉字序号("一、xxx")= 一级(替换);括号序号("（一）xxx")= 二级(同级替换);
  **罗马数字序号("Ⅰ xxx")与 "——xxx" 优先三级分类**,挂到当前二级分类下构成
  "一级-二级-三级"(如 "基础设施项目-交通-机场"),兄弟序号(Ⅱ/——航空)替换第三段,
  "（二）" 二级标题回退时不把三级段带入新分类
- "（一）电子信息" → 追加 "工业-电子信息";"（二）汽车" 同级**替换** → "工业-汽车"
- "(1)高速公路项目" 数字序号三级标题 → 同上层级规则(子序号项目名 "(3)xx水库" 不误判)
- **"——" 子分类行**(如 桂林"——铁路 2 402600",首列空 + 数量 + 金额)→ 不产生项目,
  追加为当前分类子分类("基础设施-交通"+"铁路" → "基础设施-交通-铁路");兄弟 "——航空"
  替换末段;二级标题(（二）)回退时按 dash_base 换级,不把 "——" 段带入新分类;
  分类特征:名称后带空格分隔的数量+金额(表格行),或名称命中分类名单词(文本行);
  其余(如 PDF 内容续行 "——含5座桥涵")不认作分类
- **分类行匹配前先剔除纯数字 token**(序号/数量/金额,如 "一、基础设施 64 8393887.27"
  → "一、基础设施"),避免金额数字干扰分类名判断;括号内数字("（16项）")、
  带单位数字("1.5万亿")保留(数量+金额为空格分隔的独立单元格/词)
- **行尾括号整数统计格式**(如 "城镇（含园区）基础设施类（52）"、"合计(2617)"、
  "（一）实验室体系建设（10）"):去末尾 "（N）" 后为分类名;**xlsx 行数据中该行
  有效数据项(去纯数字单元格)少于 normal_cols 才记入分类**(数据行如 "某项目（2）"
  不误判);汇总词("合计/总计/小计")仅跳过、不设分类上下文。
  **注意:该 normal_cols 保护只匹配"括号纯整数"("（52）" 无 项/个/件)——
  "（193项）" 等带量词的是分类统计后缀,走正常分类判断**(四川 2020 等
  分类名带 "（N项）" 的文件依赖此区分,否则分类行被保护分支跳过)
- **数字量词结尾格式**(如 "一、创新能力提升项目26个"、"（一）实验室体系建设项目2个",
  序号后 "名称+N个/项/件" 无空格):去末尾 "N个/项/件" 后为分类名(单格行走
  classify_single_row、多列行走 m2/m3,文本兜底 is_category_name 同步支持);
  "（三）李家岩水库" 等无统计后缀的括号序号行仍是数据行
- **分格项目数保留**(如 昆明 "一|农林水利|14|None|金额|金额"):分类行紧随文字之后
  的第一个数据项,若为整型(无小数点/金额逗号)、小于 max_project_num(=len(all_rows))、
  且后一单元格为空 → 不被 _is_number 过滤,保留进 joined;
  **分类识别成功时作为声明项目数**(declared_count,与 "（N项）" 同校验);
  计数须**紧邻左侧文字**(左侧第一格即文字)——忠县 2025 "1.制造与加工类" 等
  单格分类行的整数在 col16/18(隔 15 个空列,实为投资额),若被保留会把单格行挤成
  2 格 → classify_single_row 失效 → 分类名落入项目名候选列被当项目入库
  ("1.制造与加工类/3.能源/5.文化旅游" 三行曾整体成项目,电力行因小数被滤而正常分类);
  TODO: 数字后一单元格非空、行首数字等边界暂按普通数字过滤
- **括号统计支持 "共" 前缀**:("（共12个）"/"（共12项）" 与 "（12个）" 等价,
  所有 括号统计 的识别/剥离/声明项数(_record_declared)均支持 "共" 字
- 表格内无前缀单格分类行(如 广东"基础设施工程")→ category
- sheet 名含行业分类(如 内蒙古"装备制造")→ 初始 category
- 标题声明项数 "（共N项）" 收集,跨页去重后与实际解析条数对比输出校验,
  声明存 `skp_category_declared` 表(declared_count/actual_count,供统计核对)
- 上下文在 PDF 跨页、xlsx 跨 sheet、docx 表格与段落间共享

### PDF(`pdf_parser.py`)

1. 全量表格提取(`pdfplumber.extract_tables`),**跨页表格共享表头**(湖北等多页表只有首页有表头)
2. **质量门**:表格产出 ≥50 条 → 直接用;产出不足(单元格跨行截断)→ 回退列式/文本
3. 列式提取:按 x 坐标聚类列、序号行锚重建(山东"序号|名称|描述"排版),空名称行(调度表)过滤
4. 文本兜底(`parse_lines`):数字序号行(序号后容忍空格)、序号独占行、续行拼接、分类行更新上下文;
   忽略页眉/年份/页码/表头/附件/联系人/落款行;**页码信息清洗**("第 1 页,共 34 页" 移除)
5. **文本流质量门**:兜底结果中项目名含空格(碎片)比例 >30% → 文本流错乱,整文件弃用(提示人工)
6. 跨行单元格续行(首列空,如 广东"江门段")→ **跳过,不拼接**(合并效果差,已取消)
7. 扫描件(无文本层)解析 0 条,记入报告提示
8. 过滤:合计行;项目名 <3 字且其他信息为空;项目名尾部机构碎片清洗
   ("…建设项目教育厅" → "…建设项目"、"…物流园南宁市人民政府" → "…物流园",
   pdfplumber 垂直错位兜底;仅截"项目/工程/园/基地+机构尾"场景,无前缀的错位残留接受)

表格路径(extract_rows_from_table)与 xlsx 同构(见 XLSX 节):正文排版行 is_prose_row
(单文本格字段标签/超长段落)、极简表窄化分组 is_group_row_minimal、性质标题行
group_type_title 同步生效;文本兜底 parse_lines 过滤正文标签行
(如 "项目名称：…"/"联 系 人：…",不产生项目、不拼入上一项目名)。DOCX 与 PDF 共用
该表格/文本逻辑(docx_parser/pdf_parser 均走 base_parser)。

### DOCX(`docx_parser.py` + `docx_reader.py`)

- **内容读取共享层 docx_reader.iter_blocks**:按 body 顺序产出 段落/表格 块
  (python-docx 优先;**损坏/WPS 畸形包自动降级 lxml 读 document.xml**,如 安庆
  通知包 "no item named 'NULL'" 仍可读正文与表格);通知元信息解析(NoticeParser)
  与 docx 解析共用同一读取层
- **有表格 → 仅解析表格**(通知/计划文件的清单主体):逐表走公共
  extract_rows_from_table(装饰行/窄化分组/分类上下文等与 xlsx/pdf 共用);
  表前 标题/通知正文、表后 落款/说明 段落不参与解析
  (python-docx 顶层段落与表格分离,按 body 顺序遍历还原真实次序)
- **无表格 → 段落行级兜底 parse_lines(bare=True)**:纯文本清单支持两类——
  序号式(序号行)与 **段落式逐行条目**(如 甘肃名单 "续建项目：…（一）农业水利
  项目 → 每行项目名");bare 模式有**分区信号门控**:出现 性质组头("续建项目：")
  或 分类词尾分区标题("（一）农业水利项目",_is_section_head)后才把无序号短行
  当条目,纯通知/新闻稿 docx(无清单表格无信号)不产生假项目
- **单元格处理**:统一走 text_clean 清洗管道;**合并单元格跨列文本去重**
  (python-docx 会把合并值重复到各列,如 分类行 "一、基础设施" ×2,不去重会
  拼成 "一、基础设施 一、基础设施" 污染分类名)
- **表头别名**:如 "项目单位及名称"(廊坊 名单,单位+项目名合一列)属 project_name,
  需在 field_mapping.HEADER_ALIASES 维护,避免被 "项目单位"(construction_unit)
  包含匹配抢先

### XLSX(`xlsx_parser.py`)

- **遍历全部 sheet**(每 sheet 一个行业分类,如 内蒙古文件 11 个 sheet),跳过无表头 sheet
- 前 20 行内定位表头行(命中 ≥2 个字段,或仅命中"项目名称"如 山东两列表);表头之前的分类行计入上下文
- 数据行过滤:空行/合计行/分类行/责任单位分组行(A2);合并单元格仅首格有值(表头合并场景可接受)
- **group/分类检测的先后次序**:业务数据行(header_map 业务字段列含非纯数字文本)先排除、
  直接映射为项目;非业务行才做 分组行 → 建设性质标题 → 分类行 识别(单格行分组/分类
  判定集中在同一条非业务路径,避免误伤含业务字段的真项目;极简清单 header_map ≤1 列
  走窄化分组识别,细节见 "责任单位/地区分组行(A2)" 一节)
- **正文排版处理(与 xls/pdf/docx 同步)**:正文排版行(is_prose_row:单文本格的
  字段标签行/超长段落)与 "目录+正文"正文区(预扫描,编号结束后连续无序号且其后
  不再编号 → 整区跳过)不入库;pdf/docx 表格路径(extract_rows_from_table)、
  xls 循环同步接入,文本兜底(parse_lines)过滤正文标签行——详见 A2 一节
- .xls 与 .xlsx 解析规则同构(窄化分组/正文区/性质标题统一入口),保持双格式结果一致
- **两列式退化解析**(无表头时):第一列分类/第二列项目名;first 为**纯数字或单字符**
  (序号)→ 不设分类,沿用上下文;大段说明文本(>20字)忽略
- sheet 名清洗:去年份/通用后缀("装备制造"→分类);清洗结果**≤1 字符或超长**("总表"→"总")视为无意义;
  **Excel 默认 sheet 名不设分类**:`^(sheet|table)\s*\d*$`(忽略大小写,含 "Sheet 1"/"Table 1"/
  "Table" 等,导出/复制文件常见——曾整 sheet 项目 category='Table' 入库,如 广东 2021/2026 清单、
  广西(第一批) 173 条);xls 解析器同步处理

### 通知类文件(`notice_parser.py`)

文件名含 "通知/印发/下达" → 判定通知类。解析标题、文件编号(`〔2020〕4号`)、
发文单位(标题"关于"前)、发文时间(`2020年6月18日`)、正文(MEDIUMTEXT)、
文末附件名("附件N:xxx" 行)。正文含清单时项目照常解析入 `skp_project`,
`has_project_list` 标记,通知元信息入 `skp_notice`。

## 上传 GoFast(平台规则)

`util/file_config.py`:

```python
UPLOAD_ENABLED = None  # None=按平台自动;True/False=手动指定(手动优先)
```

- 平台自动:Windows → 不上传(开发机);Linux → 上传(生产)
- 上传失败不阻塞入库(`status=0` 标记),重试 1 次
- 上传开关已由平台规则取代早期 `--no-upload` 参数

## 复用工具模块

| 模块 | 用途 |
|------|------|
| `util/db_util.py` | `AsyncDB` 类:平台选库(Windows→local,Linux→db252)、连接池、`init_db` 建表+迁移、`save_file_with_projects` 单事务、`save_notice`、`query/execute` |
| `util/go_fast.py` | `GoFast.upload(bs, file_type)` 上传返回 URL |
| `util/file_config.py` | 默认目录 + 上传开关 |
| `util/area_id.py` | 省份 ID 常量,`main.detect_area` 按路径/文件名自动识别省份与年份 |
| `util/log_util.py` | 彩色日志,统一不用 print |
| `util/http/http_request_util.py` | aiohttp 异步上下文管理器 |

## 错误处理与幂等

- 单文件失败 → 记录继续,绝不中断整个目录任务
- 文件级去重:SHA-256 已存在则跳过(目录可重复执行)
- 所有 SQL 参数化占位符,字段白名单更新
- 通知信息/特有信息入库失败不影响主流程(记 warning)
- 特殊文件提示机制:解析失败、0 条(疑似扫描件/无清单)、质量门弃用、不支持的格式(.xls/.doc/.wps)
  均在报告列出,供人工检查
- **干扰数据名单**(`util/noise_filter.py` + `noise_sheets.json`):各省汇总表/省→地级市对照表
  (项目名或数据第 2 列为省份名且占比 ≥80%)→ 该 sheet 数据不入库,记录名单
  (文件+sheet+原因+示例),如 2025年各省重点重大项目清单 的"第一批(截止2025.01.24)" 等

## 验收标准

- [ ] 目录内全部支持格式文件被扫描,无遗漏、无重复处理
- [ ] 每个成功解析的文件在 `skp_file` 有记录且与项目 `file_id` 关联完整
- [ ] 金额/年份/分类/建设性质清洗正确,无数量词误判
- [ ] 文件特有信息按项目存 JSON 于 `skp_file_extra`,共性字段在 `skp_project`
- [ ] 通知类文件元信息完整(编号/发文单位/时间/附件)
- [ ] 重复执行同目录不产生重复数据(幂等)
- [ ] 特殊/失败文件在报告中有明确提示,不静默
